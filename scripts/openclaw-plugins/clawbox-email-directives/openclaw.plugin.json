{
  "id": "clawbox-email-directives",
  "name": "ClawBox email hooks",
  "description": "Removes EMAIL: card directives from replies leaving the box for a channel, where no card can render, and hands the owner's emailed-draft approval to ClawBox before the model sees it.",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false
  }
}
