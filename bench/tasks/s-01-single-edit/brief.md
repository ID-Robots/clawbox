# Config change for the sync daemon

In `config.js`, change `DEFAULT_PORT` from 3000 to 8080, and fix the spelling
mistake in the comment above `ACK_RETRIES`. Do not change anything else in this
folder.
