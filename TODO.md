# On ClawBox updater start we need to

# Fetch new clawbox code from repo

# Update app

```bash
# Install NVIDIA JetPack components
sudo apt update && sudo apt install nvidia-jetpack
```

```bash
# Set Jetson clocks and max performance on boot
sudo nvpmodel -m 0
sudo jetson_clocks
```

# update or install openclaw

# update or install local models

# Show loading screen in ui and show progress and what we are installing

# Show update complete when ready
